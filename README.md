# wendor_round_1_frontend

### Setup

1. install dependencies:

   ```bash
   npm i 
   ```
2. Run server*:
    ```
    npm run start
    ```

* The server is running on port=5000 and it can be adjusted in package.json 